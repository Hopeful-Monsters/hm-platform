/** Streamtime v2 search/filter pagination size. */
export const STREAMTIME_PAGE_SIZE = 200

/** Hard cap on aggregated paginated fetches (companies, jobs, etc.). */
export const STREAMTIME_MAX_RESULTS = 2_000
